<section class="bg-black text-white min-h-screen flex flex-col md:flex-row">
  <div class="w-full md:w-1/2 h-[50vh] md:h-screen sticky top-0 left-0 overflow-hidden">
    {% if section.settings.image != blank %}
      <img
        src="{{ section.settings.image | image_url: width: 1200 }}"
        alt="Bushido Origins"
        class="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-700"
      >
    {% else %}
      <div class="w-full h-full bg-neutral-900 flex items-center justify-center relative overflow-hidden">
        <div class="absolute w-[500px] h-[500px] bg-red-600 rounded-full blur-[150px] opacity-20 animate-pulse"></div>
        <h2 class="relative z-10 text-[12rem] font-heading font-bold text-white opacity-5 select-none leading-none">
          武<br>
          士<br>
          道
        </h2>
      </div>
    {% endif %}
  </div>

  <div class="w-full md:w-1/2 px-8 py-24 md:p-24 flex flex-col justify-center bg-black border-l border-gray-900">
    <span class="text-red-600 font-bold tracking-[0.3em] uppercase mb-4 text-sm">
      {{ section.settings.subtitle | default: 'Est. 2025 • Kathmandu to US' }}
    </span>

    <h1 class="text-6xl md:text-8xl font-heading font-bold uppercase leading-[0.9] mb-12">
      {{ section.settings.title | default: 'Respect the Code.<br>Own the Streets.' }}
    </h1>

    <div class="space-y-8 text-lg text-gray-400 leading-relaxed max-w-xl">
      <p>
        <strong>Bushido Global</strong> isn't just clothing. It is the bridge between the ancient discipline of the
        samurai and the modern hustle of the West.
      </p>
      <p>
        Born in the chaotic, vibrant streets of <strong>Kathmandu</strong>, we saw a gap. The world was loud, but it
        lacked honor. We took the principles of Bushido—Integrity, Courage, and Loyalty—and wove them into every thread.
      </p>
      <p>Now, we bring that spirit to the US market. We don't follow trends. We forge our own path.</p>
    </div>

    <div class="mt-16 border-t border-gray-800 pt-8">
      <p class="font-heading text-3xl text-white uppercase tracking-wider">Saurav D.</p>
      <p class="text-xs text-gray-600 uppercase tracking-widest mt-1">Founder & Creative Director</p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "About Story",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero Image"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Main Headline",
      "default": "Respect the Code.<br>Own the Streets."
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Est. 2025 • Kathmandu to US"
    }
  ]
}
{% endschema %}
